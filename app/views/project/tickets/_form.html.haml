= semantic_form_for [@project, @ticket] do |form|
  = form.semantic_errors :project, :title, :description, :sequence_number

  = form.inputs do
    = form.input :title
    = form.input :description
    = render 'cucumber_description'

  = form.buttons do
    = form.commit_button

