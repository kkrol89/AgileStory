= semantic_form_for @project do |form|
  = form.semantic_errors :name, :description
  = form.inputs do
    = form.input :name
    = form.input :point_scale, :collection => point_scales_as_form_collection, :include_blank => false
    = form.input :description

  = form.buttons do
    = form.commit_button

