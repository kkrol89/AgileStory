= semantic_form_for @user do |form|
  = form.semantic_errors :email, :password, :password_confirmation
  = form.inputs do
    = form.input :email
    = form.input :password, :required => true
    = form.input :password_confirmation, :required => true
  = form.buttons do
    = form.commit_button 'Update Profile'